import { CheckCircle2, Clock3, Flame, Trophy } from 'lucide-react';
import { Button, BottomNav, Card, ChecklistItem, ProgressBar, StatusBadge, StreakBadge } from '../components/UI';
import { achievements, challenge, initialChecklist, student } from '../data/mockData';
export default function Dashboard() { const completed = initialChecklist.filter(i => i.complete).length; return <main className="app-page"><header className="app-header"><div><p className="eyebrow">FRIDAY, AUGUST 8</p><h1>Good evening, {student.name} <span>👋</span></h1><p>Small progress tonight. Big proof tomorrow.</p></div><StreakBadge compact /></header><section className="dashboard-grid"><Card className="streak-card"><div className="streak-card__flame"><Flame size={38} fill="currentColor" /></div><div><strong>{student.currentStreak}</strong><span>DAY STREAK</span></div><p>Longest streak: <b>{student.longestStreak} days</b></p></Card><Card className="mission-card"><div className="card-top"><p className="muted-label">TODAY’S CHALLENGE</p><StatusBadge type="warning">Day 12</StatusBadge></div><h2>{challenge.title}</h2><div className="meta"><span><Clock3 size={16} /> ~{challenge.estimatedTime}</span><StatusBadge>{challenge.difficulty}</StatusBadge></div><p>{challenge.description}</p><Button to="/day/12">Continue today’s challenge</Button></Card></section><section className="dashboard-grid dashboard-grid--lower"><Card><div className="card-top"><p className="muted-label">OVERALL PROGRESS</p><strong>DAY 12 OF 60</strong></div><ProgressBar value={20} /><div className="progress-numbers"><span><b>11</b> completed</span><span><b>49</b> remaining</span></div></Card><Card className="momentum"><div><p className="muted-label">STREAK MOMENTUM</p><h2>🔥 Keep today’s momentum alive</h2></div><div className="check-list">{initialChecklist.map(item => <ChecklistItem key={item.id} item={item} locked />)}</div><p className="soft-note">{completed} of {initialChecklist.length} steps ready. Your next move is clear.</p></Card></section><section className="dashboard-section"><div className="section-heading"><div><p className="eyebrow">Milestones</p><h2>Your achievements</h2></div><Trophy size={22} /></div><div className="achievement-list">{achievements.map(item => <Card className="achievement" key={item.title}><span>{item.icon}</span><div><h3>{item.title}</h3><p>{item.note}</p></div><CheckCircle2 size={18} /></Card>)}</div></section><section className="activity"><p className="eyebrow">RECENT ACTIVITY</p><p><span>Day 11</span> <b>JavaScript Quiz App</b> · ✓ Completed yesterday</p></section><BottomNav /></main>; }
