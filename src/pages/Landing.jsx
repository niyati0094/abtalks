import { ArrowDown, Code2, Send, Sparkles } from 'lucide-react';
import { Button, Card, Logo, ProgressBar, StreakBadge } from '../components/UI';
const values = [['01', 'BUILD DAILY', 'Turn learning into consistent action.'], ['02', 'SHIP IN PUBLIC', 'Create visible proof of your work.'], ['03', 'BUILD YOUR STREAK', 'Stay accountable for 60 days.']];
const steps = ['Choose your track', "Get today's challenge", 'Build + submit', 'Keep your streak alive'];
export default function Landing() { return <main className="landing"><header className="site-header"><Logo /><Button to="/dashboard" className="header-cta">Start the challenge</Button></header><section className="hero-section"><div className="hero-copy"><p className="eyebrow"><Sparkles size={15} /> A 60-day coding challenge</p><h1>BUILD FOR 60 DAYS.<br /><em>SHOW THE WORLD.</em></h1><p className="lede">One meaningful mission every day. Build real projects, share your progress, and turn consistency into proof.</p><div className="hero-actions"><Button to="/dashboard">Start the challenge</Button><a className="text-link" href="#how-it-works">See how it works <ArrowDown size={17} /></a></div></div><Card className="hero-streak"><p className="muted-label">YOUR MOMENTUM</p><StreakBadge streak={12} /><div className="hero-streak__day"><span>DAY 12 <i>/ 60</i></span><strong>48 days to go</strong></div><ProgressBar value={20} /></Card></section><section className="section values"><div className="section-intro"><p className="eyebrow">Why ABTalks</p><h2>Make your learning<br />impossible to miss.</h2></div><div className="value-list">{values.map(([number, title, text]) => <article className="value" key={title}><span>{number}</span><h3>{title}</h3><p>{text}</p></article>)}</div></section><section className="section how" id="how-it-works"><p className="eyebrow">A clear path forward</p><h2>One mission. Every day.</h2><div className="steps">{steps.map((step, i) => <article key={step}><span>0{i + 1}</span><div className="step-dot" /><p>{step}</p></article>)}</div></section><section className="proof"><div><p className="eyebrow">Public proof</p><h2>Your progress shouldn’t disappear into your laptop.</h2><p>Every build becomes visible proof of what you’re learning—one commit and one post at a time.</p></div><div className="proof-icons"><Code2 size={32} /><b>+</b><Send size={32} /></div></section><section className="section streak-section"><Card className="streak-visual"><div><p className="eyebrow">STREAK STATUS</p><h2><span>🔥</span> 12 days</h2><p>48 days remaining</p></div><div className="day-grid">{Array.from({ length: 20 }, (_, i) => <span className={i < 12 ? 'active' : ''} key={i}>{i + 1}</span>)}</div></Card></section><section className="final-cta"><p className="eyebrow">Your next build starts now</p><h2>60 days.<br />60 builds.<br /><em>One stronger version of you.</em></h2><Button to="/dashboard">Start the challenge</Button></section><footer><Logo /><span>Build in public. Grow with intent.</span></footer></main>; }

